# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: ポケモンリボーン 日本語攻略
# トップページの <title> に「サイト名 | ここ」の形で足される。
# tagline を置かないと description が丸ごと題に入って長すぎる題になる。
tagline: "全19話＋クリア後9話の攻略と807種の図鑑"
# 検索結果のスニペットになる。各ページに description が無いときの既定値。
# 折り返すと改行がそのまま空白になり、日本語の文中に空白が入る。1行で書く。
description: "『ポケモンリボーン』(Pokémon Reborn) の日本語攻略サイト。本編19エピソードとクリア後9話の攻略手順、隠しアイテム、トレーナーの手持ちと対策、807種のポケモンの出現場所・覚えるわざ・進化条件をまとめています。"
# jekyll-seo-tag が canonical と og:url を組み立てるのに使う。
# 設定しないと手元のビルドが http://localhost:4000 を canonical に書き、
# その _site をそのまま配ると検索エンジンに存在しない URL を教えることになる。
url: "https://xephy.github.io"
# <html lang> と og:locale。既定は en-US で、日本語のページに英語を宣言していた。
lang: ja
email: your-email@example.com
# baseurl: "" # the subpath of your site, e.g. /blog
# twitter_username: jekyllrb
# github_username:  jekyll

source: ./src
destination: ./_site

github:
  is_project_page: true

# Build settings
theme: jekyll-theme-slate

plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
# sitemap.xml を出す。GitHub Pages では既定で有効にならないので明示する。
# 848ページあり、トップからのリンクだけでは全部は辿ってもらえない。
- jekyll-sitemap

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - "*_raw*"
  # Rejuvenation / Desolation は日本語化していないのでページを生成して
  # いない。画像だけが 42MB 配信され続けていたので公開対象から外す。
  # あとで翻訳するときのためにリポジトリには残す。
  - assets/images/rejuv/
  - assets/images/deso/
  # 図の編集用ソース。閲覧には使わない。
  - "*.drawio"
  - _utils/
  - "*.rb"
  - README.md
  - _arch/