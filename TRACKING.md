# 計測と広告の設定

このサイトの Google Analytics と AdSense の「あるべき状態」と、そう決めた理由。
設定を変えたらこのファイルも直す。

検索まわり（Search Console・サイトマップ・noindex）は `SEO.md` にある。

ビルドの `source` は `./src` なので、このファイルは公開されない。

**照合は `bin/check-tracking` で行う。** 下の「コードから確認できること」は
そのスクリプトが自動で見る。「管理画面にしか無いもの」は見られないので、
記録として置いてあるだけ（後述）。

---

## ID 一覧

| 用途 | 値 | 置き場所 |
| --- | --- | --- |
| GA4 測定 ID | `G-MZ5BBNN198` | `src/_includes/analytics.html` |
| AdSense パブリッシャー ID | `pub-6888504966832319` | `src/_layouts/default.html`, `src/ads.txt` |
| Search Console 所有権確認 | `google648e807086f5cb1a.html` | `src/`（front matter を付けない） |

旧 GA4 測定 ID `G-38SCFZBS8T` は**フォーク元 (BIGJRA) のもの**。
2024-09-10 のコミット `2ae37a7a` で入ったまま引き継いでいて、
2026-08-27 まで全ページのアクセスが向こうに送られていた。

---

## コードから確認できること

`bin/check-tracking` が見る項目。期待値はスクリプト側の定数と揃えてある。

- GA タグと AdSense タグが **1,541 / 1,543 ページ**に入っている
- タグが入っていないのは **`404.html`** と **`google648e807086f5cb1a.html`** の2つだけ
  - `404.html` は `layout: none`。中身の無いエラーページに広告を出すのは
    ポリシー上グレーで、Google には別枠の「エラーページ向け AdSense」がある
  - 確認ファイルは front matter 無しの静的ファイル。layout を当てると確認が外れる
- 旧測定 ID `G-38SCFZBS8T` が **0 件**
- `/ads.txt` が 1 行だけで、`pub-6888504966832319` を含む
- `404.html` の `<html>` が 1 つだけ（二重入れ子になっていない）
- `sitemap.xml` に `/404.html` が載っていない

---

## 管理画面にしか無いもの（コードからは確認できない）

**以下はスクリプトで検証できない。** 画面で設定した内容を書き写しただけなので、
実際の設定とずれている可能性がある。疑わしいときは AdSense の画面で見ること。

### 自動広告のフォーマット

| | 設定 |
| --- | --- |
| アンカー広告 | **オン**（下部のみ／1000px 超の画面には出さない） |
| サイドレール広告 | **オン**（左・右） |
| バナー広告 | **オン**（最大数は少なめ／間隔は広め） |
| Multiplex 広告 | **オン** |
| 全画面広告 (vignette) | **オフ** |
| 広告インテント | **オフ** |

- 「記事ページの候補をさらに見つける」= オフ
- 「既存の広告ユニットと一緒に最適化」= オフ（広告ユニットを1つも作っていない）
- ページ単位の除外 = `/reborn/all/`, `/404.html`

### GA4 のプロパティ設定

- **データの保持 = 14 か月**（既定の 2 か月では前年同月比が取れない）
- **Google シグナル = 無効のまま**。有効にするとデータのしきい値が効いて、
  トラフィックが少ないうちはレポートの行が消える。得られる属性データに使い道が無い
- **オーディエンス = 作らない**。リマーケティング専用で、広告を出稿しないなら無意味
- タスクアシスタントの「利用を開始する」が 6/6 にならないのは**正しい状態**

### リンク

- AdSense ↔ GA4：リンク済み（2026-08-27）
- Search Console ↔ GA4：リンク済み（2026-08-27）
  - レポートは「ライブラリ」でコレクションを**公開**しないと左メニューに出ない

---

## 判断の理由

- **広告インテントを切った** … 章ページの本文リンクが段落・リスト内だけで 94 本ある（実測）。
  攻略の内部リンクと広告リンクが見分けられなくなる
- **全画面広告を切った** … 章から章へ読み進める導線を遮る
- **`/reborn/all/` を除外した** … 全文1ページで 14,740,277 バイト（実測）。noindex 済み
- **SRI（`integrity`）を付けない** … AdSense のローダは中身が変わるうえ
  ハッシュが公開されていない。付けると読み込めなくなる
- **アンカー広告と「先頭へ」ボタンの重なり** … 広告の高さは配信されるまで分からず
  配信ごとに変わるので決め打ちしない。`src/assets/js/myscripts.js` が body 直下で
  画面下端に貼り付いている fixed 要素を実測して CSS 変数 `--overlay-bottom` に入れ、
  `src/assets/css/style.scss` の `#toTopButton` が
  `bottom: calc(20px + var(--overlay-bottom, 0px))` で使う。測れなければ 0 で従来位置
- **GTM は使わない** … 1人で開発しておりデプロイも2〜3分で通るので、
  「デプロイなしでタグを変えられる」という GTM の利点が効かない。
  逆に設定が web の管理画面だけに存在することになり、git の履歴から消える。
  AdSense はそもそも GTM のネイティブ対応が無い

---

## 上流 (BIGJRA/BIGJRA.github.io) を取り込むときの注意

- **測定 ID が `G-38SCFZBS8T` に戻っていないか必ず確認する。** `bin/check-tracking` が落ちる
- `src/google648e807086f5cb1a.html` を消さない／front matter を付けない
- `src/ads.txt` は上流に無いので、マージで消えないか見る

---

## AdSense の審査（2026-09-06 不合格）

**「有用性の低いコンテンツ」で不合格。** 「独自性のある質の高いコンテンツと
優れたユーザー エクスペリエンスをサイトで提供する」への違反として返ってきた。
**Google はどの項目で落としたか教えてくれない**ので、以下は当たりそうな順。

### 実測したサイトの中身（不合格時点）

| 種別 | 本数 | 本文量の中央値 |
| --- | --- | --- |
| 図鑑 `/reborn/mon/` | 807 | 4,629 字 |
| わざ `/reborn/move/` | 691 | 1,556 字 |
| 人が書いた章・付録・トップ | 41 | 30,567 字（章） |

自動生成が **1,499 / 1,541 = 97%**。わざページは 337 本が 1,500 字未満、
214 本が 1,000 字未満、100 本が 600 字未満。最短の `/reborn/move/sketch/` は
475 字で、実データは表の1行だけで残りは全ページ共通の定型文。

### 直したこと

1. **ポリシー系のページが1枚も無かった。** `src/` を grep して0件で、
   全ページに GA4 と AdSense のタグが入って Cookie を使うのに告知が無い状態だった。
   `src/privacy.md` (`/privacy/`) と `src/about.md` (`/about/`) を新設し、
   `src/_layouts/default.html` のフッターから全ページで辿れるようにした
2. **トップの「このサイトについて」が自分で独自性を否定していた。**
   旧文面は「原文の構成・画像・攻略方針はそのまま引き継いでいます」で終わっていた。
   品質ガイドラインは「他サイトのコンテンツを独自の価値を加えずに翻訳したもの」を
   無断複製の例として名指ししている。翻訳部分と自作部分が分かる書き方に直した

### あえて触っていないこと

- **薄い自動生成ページ。** noindex にするか中身を厚くするかは、
  再審査の結果を見てから決める。再度落ちたら次の手はここ
- **翻訳の許諾。** BIGJRA 氏からの許諾は未確認なので、
  `/about/` には許諾を得ている旨を書いていない。出典表示と
  「独自に追加した部分」の説明だけで組んである
- **メールアドレス。** 連絡先は GitHub Issues のみ。
  問い合わせフォームを置くかどうかは未決

---

## 未確認のまま残っていること

- **広告が実際に配信され始めたら、スマホで「先頭へ」ボタンが
  アンカー広告に隠れていないか目で見る。** `--overlay-bottom` の仕組みは
  AdSense が広告コンテナを `<body>` の直下に置くことを前提にしている。
  もっと深い階層に置かれた場合は測定できず 0 になり、ボタンは従来位置のまま出る
